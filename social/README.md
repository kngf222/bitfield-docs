# Social Assets

Open Graph, X, and LinkedIn preview images mirrored from `bitfield-site`.

`docs.json` points social metadata at these files so shared docs links use the same Bitfield launch card as the main site.
